These apps are bundled with the emulator to allow you to...

Patch baseROMs with Flips or Flips Auto Patcher to generate
ROMhacks.

Use HxD to hex edit ROMhack internal names or CRC's if this
benefits the experience on the emulator (development tool).

Convert ROMs to Big Endian .z64 format with tool64 to apply
patches to them to play ROMhacks on the emulator.

Encrypt a Project64 executable inside a WinRAR SFX archive.

-----------------------------------------------------------
Not included in this folder is Visual Studio or the v120_xp
toolset used to compile my application (development tools).
-----------------------------------------------------------

password is the password.